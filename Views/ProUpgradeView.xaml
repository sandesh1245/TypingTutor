<UserControl x:Class="TypingTutor.Views.ProUpgradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Border Background="#F8FAFC" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Hero Header with Round Icon Badge -->
            <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="28" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="1" Opacity="0.04"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Width="600" HorizontalAlignment="Left">
                        <Border Background="#FEF3C7" CornerRadius="12" Padding="12,4" HorizontalAlignment="Left" Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭐ " FontSize="12"/>
                                <TextBlock Text="PREMIUM (PRO) UPGRADE" Foreground="#B45309" FontSize="11" FontWeight="Black"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Unlock the Ultimate Exam Preparation Suite" Foreground="#0F172A" FontSize="24" FontWeight="Black"/>
                        <TextBlock Text="Get unlimited access to 1,000+ official government typing passages, custom file imports, AI smart key drills, and realistic exam simulations." Foreground="#64748B" FontSize="13" TextWrapping="Wrap" Margin="0,8,0,0" LineHeight="20"/>
                    </StackPanel>

                    <!-- License Status Card with Round Badge -->
                    <Border x:Name="BdrLicenseStatus" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#F8FAFC" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20" Width="240">
                        <StackPanel HorizontalAlignment="Center">
                            <Border Background="#FEF3C7" Width="36" Height="36" CornerRadius="18" Margin="0,0,0,8" HorizontalAlignment="Center">
                                <TextBlock Text="🛡️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="CURRENT STATUS" Foreground="#64748B" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtLicenseState" Text="FREE VERSION" Foreground="#D97706" FontSize="16" FontWeight="Black" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtLicenseUser" Text="Offline Candidate" Foreground="#64748B" FontSize="11" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Main Body: Feature Matrix & Activation Input -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="360"/>
                </Grid.ColumnDefinitions>

                <!-- Left Feature Matrix -->
                <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="24" Margin="0,0,20,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Free vs Pro Feature Comparison" Foreground="#0F172A" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" Margin="0,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#DCFCE7" Width="26" Height="26" CornerRadius="13" Margin="0,0,10,0">
                                                <TextBlock Text="✓" Foreground="#15803D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="1,000+ Official Exam Passages" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Pro Unlocked (10 in Free)" Foreground="#16A34A" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" Margin="0,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#DCFCE7" Width="26" Height="26" CornerRadius="13" Margin="0,0,10,0">
                                                <TextBlock Text="✓" Foreground="#15803D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="AI Smart Drills &amp; Weak Key Targeter" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Pro Exclusive" Foreground="#0284C7" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" Margin="0,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#DCFCE7" Width="26" Height="26" CornerRadius="13" Margin="0,0,10,0">
                                                <TextBlock Text="✓" Foreground="#15803D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Unlimited Custom File Import (.txt)" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Pro Exclusive" Foreground="#0284C7" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" Margin="0,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#DCFCE7" Width="26" Height="26" CornerRadius="13" Margin="0,0,10,0">
                                                <TextBlock Text="✓" Foreground="#15803D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Advanced Heatmap &amp; Error Analytics" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Pro Unlocked" Foreground="#16A34A" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#DCFCE7" Width="26" Height="26" CornerRadius="13" Margin="0,0,10,0">
                                                <TextBlock Text="✓" Foreground="#15803D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Real-Time Official Exam Simulations" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Full Access" Foreground="#16A34A" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Right License Key Activation Panel -->
                <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="24">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Border Background="#FEF3C7" Width="28" Height="28" CornerRadius="14" Margin="0,0,8,0">
                                <TextBlock Text="🔑" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Activate Premium License" Foreground="#0F172A" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Enter your Pro activation code below to unlock all features offline." Foreground="#64748B" FontSize="12" Margin="0,0,0,20"/>

                        <TextBlock Text="Activation Key Code:" Foreground="#334155" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="TxtLicenseInput" Text="TYPING-PRO-2026" Height="42" FontSize="14" FontWeight="Bold" CharacterCasing="Upper" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#0284C7" BorderThickness="1.5" Padding="12,8" Margin="0,6,0,16" VerticalContentAlignment="Center"/>

                        <Button Content="Activate Pro License" Click="BtnActivate_Click" Background="#F59E0B" Foreground="#000000" FontSize="14" FontWeight="Black" Padding="0,12" Margin="0,0,0,12" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="18"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button x:Name="BtnDeactivate" Content="Deactivate License" Click="BtnDeactivate_Click" Background="Transparent" Foreground="#64748B" BorderThickness="0" FontSize="11"/>

                        <Border Background="#F1F5F9" CornerRadius="10" Padding="12" Margin="0,24,0,0">
                            <StackPanel>
                                <TextBlock Text="TRY THIS DEMO KEY:" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                <TextBlock Text="TYPING-PRO-2026" Foreground="#0F172A" FontSize="13" FontWeight="Bold" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
