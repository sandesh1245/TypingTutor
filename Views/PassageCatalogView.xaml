<UserControl x:Class="TypingTutor.Views.PassageCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Border Background="#F8FAFC" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header & Action Button with Round Icon Badge -->
            <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="18,16" Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="1" Opacity="0.04"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Round Icon Container -->
                        <Border Background="#DCFCE7" Width="44" Height="44" CornerRadius="22" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="📚" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="OFFICIAL EXAM PASSAGE CATALOG" Foreground="#0F172A" FontSize="18" FontWeight="Black"/>
                            <TextBlock Text="1,000+ authentic exam passages (100 per language, ~1,000 words each) for Court, SSC, Railway &amp; State typing tests." Foreground="#64748B" FontSize="12" Margin="0,3,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <Button x:Name="BtnImportCustom" Click="BtnImportCustom_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#0284C7" Foreground="#FFFFFF" FontWeight="Bold" Padding="18,9" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="18"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#38BDF8" Width="20" Height="20" CornerRadius="10" Margin="0,0,8,0">
                                <TextBlock Text="+" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                            </Border>
                            <TextBlock Text="Import Custom Passage" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Filter Bar with Round Search & Badges -->
            <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="12" Padding="14,10" Margin="0,0,0,16" BorderBrush="#E2E8F0" BorderThickness="1">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Language Filter -->
                        <Border Background="#F1F5F9" Width="26" Height="26" CornerRadius="13" Margin="0,0,6,0">
                            <TextBlock Text="🌐" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Language:" Foreground="#64748B" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox x:Name="CmbLanguage" Width="145" Height="32" SelectionChanged="Filter_SelectionChanged" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Margin="0,0,16,0" VerticalContentAlignment="Center"/>

                        <!-- Category Filter -->
                        <Border Background="#F1F5F9" Width="26" Height="26" CornerRadius="13" Margin="0,0,6,0">
                            <TextBlock Text="🏷️" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Category:" Foreground="#64748B" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox x:Name="CmbCategory" Width="150" Height="32" SelectionChanged="Filter_SelectionChanged" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Margin="0,0,16,0" VerticalContentAlignment="Center"/>

                        <!-- Difficulty Filter -->
                        <Border Background="#F1F5F9" Width="26" Height="26" CornerRadius="13" Margin="0,0,6,0">
                            <TextBlock Text="⭐" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Difficulty:" Foreground="#64748B" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox x:Name="CmbDifficulty" Width="105" Height="32" SelectionChanged="Filter_SelectionChanged" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Margin="0,0,16,0" VerticalContentAlignment="Center"/>

                        <!-- Search -->
                        <Border Background="#F1F5F9" Width="26" Height="26" CornerRadius="13" Margin="0,0,6,0">
                            <TextBlock Text="🔍" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBox x:Name="TxtSearch" Width="150" Height="32" TextChanged="TxtSearch_TextChanged" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Padding="8,5" FontSize="11" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <Border Background="#EEF2FF" CornerRadius="12" Padding="10,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtPassageCount" Text="Showing 1,000 Passages" Foreground="#4F46E5" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Passages List Grid -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="LstPassageCards">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Width="940"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFFFF" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Width="295" Height="175" Margin="0,0,14,14" Padding="14">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Title & Round Icon Badge -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Background="#F1F5F9" Width="28" Height="28" CornerRadius="14" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <TextBlock Text="📄" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontFamily="Nirmala UI, Mangal, Segoe UI" Foreground="#0F172A" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    </Grid>

                                    <!-- Content Preview -->
                                    <TextBlock Grid.Row="1" Text="{Binding Content}" FontFamily="Nirmala UI, Mangal, Segoe UI" Foreground="#475569" FontSize="11" TextWrapping="Wrap" TextTrimming="WordEllipsis" Margin="0,8,0,8" LineHeight="16"/>

                                    <!-- Bottom Metadata & Select Button -->
                                    <Grid Grid.Row="2">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#EEF2FF" CornerRadius="8" Padding="7,3" Margin="0,0,4,0">
                                                <TextBlock Text="{Binding LanguageDisplayName}" Foreground="#4F46E5" FontSize="9" FontWeight="Bold"/>
                                            </Border>
                                            <Border Background="#E0F2FE" CornerRadius="8" Padding="7,3" Margin="0,0,4,0">
                                                <TextBlock Text="{Binding Category}" Foreground="#0284C7" FontSize="9" FontWeight="SemiBold"/>
                                            </Border>
                                            <Border Background="#F8FAFC" CornerRadius="8" Padding="6,3">
                                                <TextBlock Text="{Binding WordCount, StringFormat='{}{0} W'}" Foreground="#64748B" FontSize="9" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>

                                        <!-- Pill Round Practice Button -->
                                        <Button Content="Practice ▶" Click="BtnPracticePassage_Click" Tag="{Binding}" HorizontalAlignment="Right" Background="#0284C7" Foreground="#FFFFFF" BorderThickness="0" Padding="14,5" FontSize="11" FontWeight="Bold">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="13"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Pagination Controls Bar with Rounded Pills -->
            <Border Grid.Row="3" Background="#FFFFFF" CornerRadius="12" Padding="14,10" Margin="0,10,0,0" BorderBrush="#E2E8F0" BorderThickness="1">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#F1F5F9" Width="24" Height="24" CornerRadius="12" Margin="0,0,8,0">
                            <TextBlock Text="📊" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="TxtPageSummary" Text="Showing 1 to 24 of 1,000 official passages" Foreground="#64748B" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="BtnPrevPage" Content="◀ Previous" Click="BtnPrevPage_Click" Background="#F1F5F9" Foreground="#334155" BorderBrush="#CBD5E1" Padding="14,6" FontWeight="SemiBold" FontSize="11" Margin="0,0,8,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="14"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Border Background="#E0F2FE" CornerRadius="14" Padding="14,5" Margin="0,0,8,0">
                            <TextBlock x:Name="TxtCurrentPageInfo" Text="Page 1 of 42" Foreground="#0284C7" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        </Border>
                        <Button x:Name="BtnNextPage" Content="Next ▶" Click="BtnNextPage_Click" Background="#F1F5F9" Foreground="#334155" BorderBrush="#CBD5E1" Padding="14,6" FontWeight="SemiBold" FontSize="11">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="14"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Import Custom Passage Modal Overlay -->
            <Border x:Name="BdrImportModal" Grid.Row="0" Grid.RowSpan="4" Background="#800F172A" Visibility="Collapsed">
                <Border Background="#FFFFFF" CornerRadius="18" BorderBrush="#CBD5E1" BorderThickness="1" Padding="26" Width="550" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#E0F2FE" Width="36" Height="36" CornerRadius="18" Margin="0,0,10,0">
                                <TextBlock Text="📝" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Import Custom Passage" Foreground="#0F172A" FontSize="18" FontWeight="Black"/>
                                <TextBlock Text="Add your own text document or paste legal/exam passage content." Foreground="#64748B" FontSize="12" Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Language / Script:" Foreground="#334155" FontSize="11" FontWeight="Bold"/>
                        <ComboBox x:Name="CmbImportLanguage" Height="34" Margin="0,4,0,12" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1"/>

                        <TextBlock Text="Passage Title:" Foreground="#334155" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="TxtCustomTitle" FontFamily="Nirmala UI, Mangal, Segoe UI" Height="34" Margin="0,4,0,12" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Padding="8,4"/>

                        <TextBlock Text="Passage Content Text:" Foreground="#334155" FontSize="11" FontWeight="Bold"/>
                        <TextBox x:Name="TxtCustomContent" FontFamily="Nirmala UI, Mangal, Segoe UI" Height="140" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,4,0,16" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" Padding="8"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancel" Click="BtnCancelImport_Click" Background="#F1F5F9" Foreground="#475569" BorderBrush="#CBD5E1" Padding="18,7" Margin="0,0,10,0">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="15"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                            <Button Content="Save &amp; Add Passage" Click="BtnSaveCustomPassage_Click" Background="#0284C7" Foreground="#FFFFFF" FontWeight="Bold" Padding="20,7" BorderThickness="0">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="15"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Border>
</UserControl>
