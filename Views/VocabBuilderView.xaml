<UserControl x:Class="TypingTutor.Views.VocabBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Border Background="#F8FAFC" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Round Icon & Language Dropdown -->
            <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="18,14" Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#FEF3C7" Width="44" Height="44" CornerRadius="22" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="⚡" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="SPECIALIZED VOCABULARY BUILDER" Foreground="#0F172A" FontSize="17" FontWeight="Black"/>
                            <TextBlock Text="High-frequency exam words, administrative terminology &amp; legal vocabulary across 10 Indian languages." Foreground="#64748B" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Border x:Name="BdrVocabIndicBadge" Background="#DCFCE7" CornerRadius="12" Padding="10,4" Margin="0,0,12,0" BorderBrush="#86EFAC" BorderThickness="1" Visibility="Collapsed" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⌨️ " FontSize="11"/>
                                <TextBlock x:Name="TxtVocabIndicMode" Text="Indic In-App IME: Active" Foreground="#15803D" FontSize="11" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#F1F5F9" Width="30" Height="30" CornerRadius="15" Margin="0,0,8,0" VerticalAlignment="Center">
                            <TextBlock Text="🌐" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ComboBox x:Name="CmbDrillCategory" Width="260" Height="36" SelectionChanged="CmbDrillCategory_SelectionChanged" Background="#F8FAFC" Foreground="#0F172A" BorderBrush="#CBD5E1" VerticalContentAlignment="Center" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Body Arena -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Drill Selection Panel -->
                <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Padding="14" Margin="0,0,16,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="4,0,0,12">
                            <Border Background="#FEF3C7" Width="22" Height="22" CornerRadius="11" Margin="0,0,6,0">
                                <TextBlock Text="⚡" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Drill Modules" Foreground="#334155" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ListBox x:Name="LstDrills" Grid.Row="1" SelectionChanged="LstDrills_SelectionChanged" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" CornerRadius="10" Padding="12" Margin="0,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontFamily="Nirmala UI, Mangal, Segoe UI, Arial" Foreground="#0F172A" FontSize="13" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Category}" Foreground="#0284C7" FontSize="10" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding Description}" FontFamily="Nirmala UI, Mangal, Segoe UI, Arial" Foreground="#64748B" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Typing Practice Workspace -->
                <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Active Word Target Display -->
                        <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Padding="24" Margin="0,0,0,16">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="CURRENT TARGET WORD / SYMBOL" Foreground="#64748B" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="TxtCurrentWord" Text="government" Foreground="#0284C7" FontSize="36" FontWeight="Black" FontFamily="Nirmala UI, Mangal, Segoe UI, Consolas, Arial" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                <TextBlock x:Name="TxtNextWords" Text="Next: development, constitution, parliament" Foreground="#64748B" FontSize="12" FontFamily="Nirmala UI, Mangal, Segoe UI, Arial" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Practice Input Area -->
                        <Grid Grid.Row="1" VerticalAlignment="Center">
                            <StackPanel>
                                <TextBlock Text="Type the exact word above and press Space or Enter (Indic In-App IME enabled):" Foreground="#475569" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                <Border Width="440" Height="54" Background="#F8FAFC" CornerRadius="27" BorderBrush="#0284C7" BorderThickness="2">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#0284C7" BlurRadius="10" ShadowDepth="1" Opacity="0.15"/>
                                    </Border.Effect>
                                    <TextBox x:Name="TxtVocabInput" KeyDown="TxtVocabInput_KeyDown" PreviewTextInput="TxtVocabInput_PreviewTextInput" FontFamily="Nirmala UI, Mangal, Segoe UI, Consolas, Arial" FontSize="22" FontWeight="Bold" TextAlignment="Center" VerticalContentAlignment="Center" Background="Transparent" Foreground="#0F172A" BorderThickness="0"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- Progress Statistics Ticker with Round Icons -->
                        <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="12" Padding="14" BorderBrush="#E2E8F0" BorderThickness="1">
                            <UniformGrid Columns="3">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#E0F2FE" Width="36" Height="36" CornerRadius="18" Margin="0,0,10,0">
                                        <TextBlock Text="✅" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="WORDS COMPLETED" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock x:Name="TxtCompletedCount" Text="0 / 40" Foreground="#0F172A" FontSize="18" FontWeight="Black"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#DCFCE7" Width="36" Height="36" CornerRadius="18" Margin="0,0,10,0">
                                        <TextBlock Text="🎯" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="RHYTHM SCORE" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock x:Name="TxtRhythmScore" Text="98.5%" Foreground="#16A34A" FontSize="18" FontWeight="Black"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#FEE2E2" Width="36" Height="36" CornerRadius="18" Margin="0,0,10,0">
                                        <TextBlock Text="❌" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="WORD ERRORS" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock x:Name="TxtWordErrors" Text="0" Foreground="#DC2626" FontSize="18" FontWeight="Black"/>
                                    </StackPanel>
                                </StackPanel>
                            </UniformGrid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
